# 断裂融合项目正确性修复与诚实评估计划

## 1. 实验身份与范围

- 分支：`codex/correctness-validation-v1`
- 工作树：当前目录
- 基线提交：`d74e669c63e1f3cde21eafbb039023fb53ff0ef5`
- 实验层级：先做 `auxiliary/dev` 正确性修复；通过后再做 `main/test` 三期数据回归
- 本轮主张：修复图路径权重语义，并把 Stage 1 的训练拟合分数替换为可审计的空间分块评估。
- 本轮不做：GNN、GUI扩展、SHAP美化、代理模型 `bc_rel` 改造、商业指标包装、无外部标签的油气命中率宣传。

## 2. 研究问题与假设

### 2.1 图路径权重

- 研究问题：`NC_A` 等“连通能力”作为 NetworkX 最短路径权重时，是否错误地把高连通边当成长距离？
- 零假设：直接使用容量权重不影响关键节点排序。
- 备择假设：应将容量转换为阻抗 `1 / (capacity + eps)`；修复会改变加权 betweenness 标签与关键节点排序，但不应改变渗流模拟按容量删边的定义。

### 2.2 Stage 1 评估

- 研究问题：当前 XGBoost `fit(X, y)` 后在同一 `X` 上计算的 R² 是否能代表泛化？
- 零假设：训练拟合 R² 可作为阶段一性能指标。
- 备择假设：该值只能标为训练拟合；主报告应使用按空间网格分组的交叉验证，并同时报告均值、标准差、折数和失败原因。

## 3. 基线与比较契约

- 数据：仓库现有海西期、喜山期、印支燕山期三套网格数据，不更换、不抽样。
- 当前代理模型基线：
  - 随机划分 R²：0.7788
  - 空间块 CV R²：0.4239 ± 0.1866
  - LOPO R²：-0.7847 ± 0.7067
- 当前 Stage 1 汇总 R²：0.2537 / 0.4693 / 0.6651；经代码审计确认为训练集拟合值，不作为泛化基线。
- 当前测试基线：55 tests passed。
- 比较边界：修复前后使用同一数据、同一网格配置、同一随机种子；新增空间 CV 是补充诚实口径，不与旧训练拟合值混称同一指标。

## 4. 代码变更图

| 路径 | 当前角色 | 计划修改 | 必要性 | 风险控制 |
|---|---|---|---|---|
| `program/percolation.py` | 构图、渗流、关键节点 | 同一条边同时保存 `capacity` 与 `distance`；渗流使用容量，最短路径中心性使用阻抗 | 修复容量/距离语义混淆 | 保留兼容 `weight` 字段；用单调性测试锁定 |
| `program/agent_model.py` | 精确 BC 标签和代理模型 | 精确 BC 显式使用 `distance`；评估输出记录标签权重语义 | 防止代理模型学习错误标签 | 不改数据拆分和模型参数 |
| `program/batch_run.py` | Stage 1 XGBoost | 保留训练拟合仅作诊断；新增空间分组 CV，汇总以 CV 为主 | 避免训练集 R² 冒充泛化 | 小样本/退化坐标返回明确状态，不伪造分数 |
| `tests/test_path_weight_semantics.py` | 新增 | 容量到阻抗、中心性路径选择、渗流容量保留测试 | 防止语义回归 | 使用微型确定性图 |
| `tests/test_batch_spatial_cv.py` | 新增 | 空间块构造、CV输出、退化输入测试 | 锁定诚实评估合同 | 合成数据、小模型、固定种子 |
| `artifacts/experiment/correctness-v1/` | 新增 | 记录命令、环境、基线、结果与自审 | 保证可复核 | 不写不存在的结果 |

## 5. 实施顺序

1. 写失败测试，证明容量越大时阻抗应越小，且最短路径应优先高容量边。
2. 最小修改构图字段；渗流仍按容量排序，PageRank仍按强度，betweenness改用阻抗。
3. 写 Stage 1 空间块 CV 失败测试；抽取纯评估函数。
4. 将汇总 CSV 改为同时包含 `train_r2`、`spatial_cv_r2_mean/std`、折数和状态，禁止只写一个含义不明的 `XGBoost R²`。
5. 运行新增测试、55项原测试、导入冒烟。
6. 在真实三期数据上重跑相关阶段，保存日志和结构化结果。
7. 对比关键节点、代理模型三种评估、Stage 1 空间 CV；给出支持、反驳或不确定结论。

## 6. 验收与停止门槛

### 最低证据

- 新测试先红后绿；原55项测试零回归。
- 图边同时存在容量与阻抗，且两者语义有单测。
- Stage 1 不再把训练拟合值作为唯一性能指标。

### 扎实证据

- 三期真实数据均产出有限的空间 CV 指标与折数。
- 修复后的关键节点结果可解释；任何大幅变化均记录而不掩盖。
- 运行命令、Python版本、依赖、随机种子、输出路径完整。

### 停止/转向

- 若阻抗修复导致图结果不稳定，先做权重定义敏感性分析，不继续代理模型。
- 若空间 CV 明显低于训练拟合，不调参粉饰，直接降低 Stage 1 主张。
- 若 LOPO 在后续 `bc_rel` 改造后仍为负，停止跨期代理路线，保留精确计算。
- 在没有井位、产量、储层或专家盲评标签前，不宣称油气预测命中率。

## 7. 预期输出

- `artifacts/experiment/correctness-v1/RUN.md`
- `artifacts/experiment/correctness-v1/baseline.json`
- `artifacts/experiment/correctness-v1/result.json`
- `artifacts/experiment/correctness-v1/test.log`
- 修订后的三期结果文件（使用新文件名，不覆盖基线文件）

## 8. 实施前自审

1. **方向是否过宽**：已收窄为两个可证伪的正确性问题；外部地质验证不伪装成本轮已有数据。
2. **是否混淆指标**：训练拟合、随机划分、空间 CV、LOPO 分开命名，不横向偷换。
3. **是否一次改太多变量**：先修权重语义，再改评估；每一步有独立测试和提交点。
4. **是否影响基线可比性**：数据、配置与种子保持不变；标签语义变化明确标记为“修复后新口径”。
5. **是否存在反向激励**：不设置“R²必须提高”；本轮成功标准是评估可信，即使分数下降也可判定修复成功。
6. **是否会覆盖用户资产**：在独立工作树和分支实施；真实输出使用新目录，原仓库与旧结果保留。
7. **是否有错误成功条件**：测试通过只证明实现正确，不等于油气预测有效；外部标签仍是后续硬门槛。
8. **最强替代解释**：指标下降可能来自消除泄漏，而非模型退化；关键节点变化可能来自纠正距离语义，而非算法不稳定。

## 9. 计划修订规则

- 若测试暴露当前 `weight` 字段被多处以不同语义复用，先补调用点清单再修改，不做全局搜索替换。
- 若真实运行超过10分钟，先保存阶段日志并检查是否仍有进展；不因耗时自动改用近似算法。
- 任何数据、拆分或指标定义变化必须先更新本计划，再执行。

## 10. 修订记录

| 时间 | 变化 | 原因 | 对可比性的影响 |
|---|---|---|---|
| 2026-08-23 | 暂缓原仓库规划的 `bc_rel` 改造 | 修正阻抗标签后LOPO均值已达0.3066，超过原0.2成功线 | 避免再次改变目标，保留本轮单变量归因 |
| 2026-08-23 | 下一步增加Top-K/Spearman而非继续调总体R² | 分段R²显示中高段仍为负 | 收窄主张到整体近似；高值节点单独验收 |
| 2026-08-23 | validation-v2 预运行因依赖自动升级作废并钉回父实验核心版本 | XGBoost 3.4.1使LOPO均值轻微漂移 | 正式结果使用XGBoost 3.2.0等父实验版本，高可比 |
| 2026-08-23 | 代理排序失败后停止网格/聚类扩展切片 | 三期Top20重合率均低于0.50 | 按预设停止门槛关闭代理主线，避免无效扩展 |
| 2026-08-23 | 正式匹配由DBSCAN改为带直径约束的一对一匹配 | 首次真实运行49簇发生链式合并，累计丢弃8330节点 | 首次3靶区结果作废；最终9872节点全部守恒且同一期零重复 |
| 2026-08-23 | 靶区连续聚类增加18km最大直径 | 中间结果单区吞并621/696候选单元，失去决策意义 | 最终拆为59个空间单元组，稳定9个、不稳定50个 |
| 2026-08-23 | 不稳定候选停止一二三级包装 | 中间3靶区稳定率仅0.49至0.56 | 稳定率低于0.80统一标记“不稳定候选”，图件仅重点标注稳定区 |
| 2026-08-23 | 正式入口升级为首屏主操作卡；日志与摘要合并为可收起页签；图件默认自适应 | 用户实测认为菜单入口不醒目、固定日志/摘要挤压画布、图件需滚动查看 | 保留同一正式后端与旧图件功能，只收敛交互层级和查看器 |
| 2026-08-23 | 移除启动模态提示与`QThread.terminate()` | 无界面验证被旧提示阻塞，强制终止Qt进程触发Windows原生错误；硬终止线程本身也不安全 | 改为非模态快速提示和协作式安全取消，活跃线程结束前阻止窗口销毁 |
| 2026-08-23 | P3 质量平衡验收采用边界通量相对差 0.5% | 单情景 PorePy 冒烟有限值通过，边界通量相对差为 0.106%；原计划的 1e-6 不符合该非结构混合维网格的后处理量精度 | 只调整后处理验收阈值，不放宽求解器收敛；仍逐情景报告原始残差 |
| 2026-08-23 | P3 改用全局离散方程残差作为求解验收 | 6 情景均求解完成，但二维基质边界通量差随高渗透率对比升至 6.6%，该切片未覆盖混合维系统全部守恒项 | 不再继续放宽边界通量阈值；边界差降为诊断警告，主验收使用 `equation_system` 全局残差 |
| 2026-08-24 | P2 固定网格步长场景排除出统计分母 | 首个 2700 m 场景使三期图均变为 0 边，证明成品 3000 m 网格 CSV 不能靠修改配置模拟重新网格化 | 保留预注册场景与排除证据；网格尺度敏感性等待同坐标系原始断裂线后从源头重建 |
| 2026-08-24 | P2 主出现判据由 9 km 质心匹配改为 25% 几何覆盖 | 自审显示质心半径 3/4.5/6/9 km 对稳健数量影响显著，且一对一质心匹配不能正确处理靶区分裂/合并 | 几何覆盖决定出现频率；同时导出 25%/50% 两档，质心匹配仅用于分数、排名和漂移 |
| 2026-08-24 | 靶区默认最大直径由 18 km 收敛到 15 km | 合成真值 v1 恢复全部真值单元但基准靶区质心误差 5.14 km；单因素跟进中 15 km 使误差降至 3.25 km，且既有 P2 `diameter_low` 对原基准靶区召回 1.0、靶区数增幅 37.3% 未超过预设 50% | 保留 v1 失败证据；15 km 作为新基准重新运行正式筛选与 P2，12/9 km 仅保留为碎片化边界 |

## 13. P2 参数不确定性与 P3 流动物理试验合同

### 13.1 当前决策与主张边界

- 用户决定暂时跳过依赖外部井位、储层或专家标签的 P1，直接执行 P2 和 P3。
- P2 回答“候选区是否依赖某一个参数设定”，不回答“候选区是否真实含油气”。
- P3 使用 PorePy 1.13 做二维单相流方法试验；在当前三期数据只有网格统计、缺少同坐标系原始断裂线和渗透率/孔隙度/边界条件的情况下，P3 结果保持为独立的物理可行性证据，不进入候选综合分。
- 仓库中的 `KB11` 裂缝线数据仅用于验证几何转换、网格化和求解链路，不能冒充塔里木工区验证数据。

### 13.2 P2 研究问题、假设与场景

- 研究问题：基准候选靶区在合理的邻接容差、跨期匹配容差、候选分位数、聚类尺度、最大直径、距离变换基准和评分权重扰动下，是否仍被重复识别？
- 零假设：候选区出现频率低、排序区间宽，当前靶区主要由单一参数设定驱动。
- 备择假设：主要候选区在大部分预注册场景中可匹配复现，且得分、排名和质心漂移区间可控。
- 主指标：基准靶区候选出现频率、内部稳定出现频率、得分 5%/50%/95% 分位、排名区间、质心漂移 95% 分位。
- 场景采用预注册的单因素扰动，不做看到结果后的追逐式调参：邻接容差 75/300 m、匹配容差 1200/1800 m、候选分位数 0.75/0.85、聚类 eps 3750/5250 m、最大直径 15000/21000 m、拓扑或持续性权重增强、`inverse_sqrt`/`neglog` 作为基准距离变换。
- 稳健门槛是项目工程门槛：候选出现频率至少 0.80 且得分区间有限时才标为 `robust_internal`；0.50–0.80 标为 `conditional_internal`；低于 0.50 标为 `fragile_internal`。

### 13.3 P3 研究问题、假设与情景

- 研究问题：代表性二维裂缝几何在不同压差方向和裂缝/基质渗透率对比下，PorePy 混合维单相流求解能否稳定收敛，并给出方向性有效导流响应？
- 零假设：求解链路不可复现，或方向/导流能力扰动不产生可解释的有限响应。
- 备择假设：至少两个边界方向、多个裂缝导流能力情景均完成求解，质量守恒/有限值检查通过，输出可复算。
- 试验数据：`program/KB11/KB11_traces_100.geojson` 与 `KB11_area.geojson`；保留其 EPSG:3067 元数据，并明确这是方法演示几何。
- 试验情景：西→东与南→北两种压差方向；裂缝渗透率相对基质的低/中/高三档；其数值是无量纲化情景假设，不是实测储层参数。
- 主指标：求解状态、网格单元数、压力范围、边界通量/等效导流响应、方向各向异性比、情景单调性与质量平衡残差。
- 停止门槛：Windows/Python 3.12 下 PorePy 或 Gmsh 无法在隔离环境导入，记录 `external_dependency_blocked`；几何清洗后仍无法网格化则停止扩大网络；同一失败类两次且无新证据时不继续重试。

### 13.4 最小代码变更图

| 路径 | 计划修改 | 验证 |
|---|---|---|
| `program/uncertainty_analysis.py` | 生成固定场景、执行/复用时期指标、跨情景匹配、统计区间和敏感度 | 合成靶区匹配、缺失场景、频率/分位数单测 |
| `scripts/run_uncertainty_p2.py` | P2 唯一 CLI，保存配置快照、日志、CSV/JSON/PNG | 微型管线冒烟与真实三期运行 |
| `program/porepy_flow_pilot.py` | 几何清洗、PorePy 可选导入、模型/边界/渗透率情景、指标抽取 | 无依赖时清晰阻塞；安装后小几何求解 |
| `scripts/run_porepy_p3.py` | P3 唯一 CLI 与结构化状态输出 | 代表性 KB11 试验 |
| `requirements-physics.txt` | 隔离固定 `porepy==1.13.0`，不污染正式 GUI 环境 | 导入和版本检查 |
| `tests/test_uncertainty_analysis.py` | P2 统计和空间匹配合同 | 确定性、有限值、边界情况 |
| `tests/test_porepy_flow_pilot.py` | P3 几何预处理和依赖边界 | 默认环境不要求安装 PorePy |
| `artifacts/experiment/p2-p3-v1/` | 计划、环境、命令、日志、结果、自审 | 产物清单和主张核对 |

### 13.5 执行层级与停止决策

- 最低证据：P2/P3 新模块、CLI 和测试通过；P3 缺依赖时返回结构化阻塞，不产生伪结果。
- 扎实证据：P2 在真实三期数据完成全部预注册场景；P3 在隔离 PorePy 环境完成至少 2×3 个情景，并通过有限值和质量平衡检查。
- P2 若大多数基准靶区出现频率低于 0.50，结论是“当前靶区参数脆弱”，停止包装稳定等级，回到数据/定义审查。
- P3 即使通过也只支持“方法链路可用”；没有同坐标系工区断裂线和实测参数前，不支持“现有候选区获得物理验证”。

## 11. validation-v2 排名与敏感性验证

### 11.1 父结果与核心问题

- 父结果：`correctness-v1`（提交 `d69b7a3`），修正口径下代理模型 LOPO R² 为 `0.3066±0.1588`，但随机测试集的中值段和高值段 R² 为负。
- 核心问题：代理模型能否在未见时期中稳定筛出精确 betweenness 的高值节点；若不能，是否至少可安全地用于粗筛。
- 本轮主张边界：只验证“拓扑高值节点排序近似”，不把 betweenness 等同于油气发现概率。

### 11.2 假设、指标与判定门槛

- 零假设：LOPO 预测与精确中心性排序的一致性不足，代理模型不能承担跨期高值节点筛选。
- 备择假设：三个留出时期均达到可复用的排序一致性。
- 主指标：逐期 Spearman、Kendall、Top 5%/10%/20% 重合率、NDCG@5%/10%/20%。
- `go`：每期 Spearman ≥ 0.70 且 Top 20% 重合率 ≥ 0.70。
- `conditional`：所有时期 Spearman ≥ 0.50 且 Top 20% 重合率 ≥ 0.50，但未达到 `go`；只允许代理粗筛后对候选区精确重算。
- `stop_proxy`：任一时期 Spearman < 0.50 或 Top 20% 重合率 < 0.50；跨期代理路线停止。
- 以上门槛是本项目的工程决策门槛，不宣称为通用地学标准。

### 11.3 分析切片与固定条件

| slice | 变化因素 | 固定因素 | 输出 | 停止条件 |
|---|---|---|---|---|
| `rank-lopo` | 新增排序指标，不改模型 | 三期数据、LOPO拆分、XGBoost参数、随机种子42 | 逐期与聚合排序JSON/CSV、对照图 | 得出 go/conditional/stop_proxy 后不继续调模型 |
| `distance-transform` | `inverse`、`inverse_sqrt`、`neglog` 三种容量到距离转换 | 图邻接、NC_A容量、网格步长3000m、精确BC算法 | 各变体相对基准的逐期 Spearman/Top-K | 若任一合理变体 Top20%重合率<0.70，标记标签定义脆弱 |

`neglog` 先将容量按当期正容量最大值缩放到 `(0,1]`，再计算 `-log(w_norm)+eps`；只用于敏感性分析，不替换当前基准定义。

### 11.4 执行资源与边界

- 设备：CPU，本地 Python；数据约三期共万级网格。
- 预计：LOPO 排名评估复用一次训练数据与三折模型；距离转换需要三种精确中心性计算，是本轮主要耗时。
- 若单个敏感性变体超过10分钟且无进展，保存日志并缩减为逐期确定性子集；不得把子集结果冒充完整三期结果。
- 外部井位、产量和专家盲评仍缺失；本轮不能解除“油气预测有效性”阻塞。

### 11.5 代码与产物

- `program/agent_model.py`：新增可单测的排序指标，并写入LOPO逐期结果。
- `program/percolation.py`：允许显式选择距离转换，默认行为保持 `inverse`。
- `scripts/run_validation_v2.py`：唯一正式入口，生成结果、CSV、图和运行环境记录。
- `tests/test_agent_ranking.py`、`tests/test_path_weight_semantics.py`：锁定指标和距离转换语义。
- `artifacts/experiment/validation-v2/`：`result.json`、`ranking_metrics.csv`、`distance_sensitivity.csv`、图、日志、`VALIDATION.md`。

### 11.6 自审规则

1. 排名评估必须使用LOPO留出预测，禁止用训练集预测支撑泛化主张。
2. Top-K 并列值使用稳定排序和固定索引打破并列，保证复现；同时记录K的实际样本数。
3. 敏感性比较针对精确标签本身，不与代理模型性能混合解释。
4. 负面或矛盾结果必须进入最终报告，不能因总体R²尚可而隐藏。
5. 若排序门槛失败，本轮立即给出降级或停止结论，不开启GNN、调参或标签重构。

## 12. target-screening-mvp-v1 实施合同

### 12.1 当前目标

- 父提交：`d1ad299`；因共享Git元数据权限不足，无法新建分支，继续使用现有独立工作树，不修改原始D盘工作目录。
- 产品目标：在保留现有全部可用绘图与专业分析功能的前提下，新增唯一正式主流程“精确分析 → 多期匹配 → 候选靶区 → 稳定性 → 证据卡 → 导出”。
- 正式主张：输出内部拓扑证据支持的候选勘探有利区；无外部标签时不得声称油气预测命中。
- 本轮不做：删除旧图件功能、重写整个PyQt界面、恢复代理模型决策、GNN、虚构外部验证或示例行业指标。

### 12.2 真实架构收敛

- `screening_pipeline.py` 是唯一正式编排入口；GUI和CLI均调用它。
- `percolation.py` 继续提供图和路径语义基础能力；正式管线计算精确betweenness、PageRank和节点移除连通影响。
- `multiperiod_overlay.py` 的旧三期叠加与绘图入口保留；正式管线增加可覆盖两期/三期的坐标聚类匹配，不重复开发第二个GUI。
- `fusion_algorithm.py` 的旧实验方法保留；正式靶区评分使用透明分项公式，不调用XGBoost/GAT/AE/VAE。
- GUI保留现有原图、分类图、热力图、方位图、玫瑰图、三元图、关系图、拓扑图及实验入口；正式菜单与研究菜单分离。

### 12.3 评分与证据合同

- 时期内百分位标准化，避免量纲混合。
- 网络关键性 = `0.70 × exact_betweenness_percentile + 0.30 × pagerank_percentile`。
- 综合分 = `0.35 × 网络关键性 + 0.25 × 节点移除影响 + 0.25 × 多期持续性 + 0.15 × 距离转换Top20稳定率`。
- 外部井位或专家结果不进入内部评分，只作为独立验证层。
- 候选单元默认要求：综合分达到80%分位、至少两个时期支持、至少一个时期精确betweenness进入Top20%。
- 允许无候选结果；禁止降低阈值凑出靶区。

### 12.4 验收门槛

- 当前74项测试零回归，并新增合同、评分、聚类、外部验证和端到端测试。
- 合成三期数据能识别预置高值区，删除一期支持后持续性下降。
- 正式主流程不导入或调用 `agent_model`。
- 同一输入和配置产生稳定target_id、分项得分和artifact schema。
- 真实三期运行产出有限指标、候选结果或合法空结果、运行清单、日志、CSV/JSON/PNG。
- GUI原绘图按钮绑定保持不变；新增一键入口实际调用统一主流程；代理入口保留在研究子菜单。
- 任何外部数据缺失均写为 `not_validated`，不生成命中率。

### 12.5 停止门槛

- 若精确三变换完整运行超出可接受时间，先保留可取消进度和缓存，不改用未验证代理结果。
- 若候选区对合理变换不稳定，输出“不稳定候选”，不继续包装等级。
- 若现有GUI布局文件与运行时生成文件不一致，本轮只做菜单级整合，避免破坏全部旧按钮。
- 若外部标签不存在，外部验证模块完成合同和空状态即可，不伪造演示数据。
