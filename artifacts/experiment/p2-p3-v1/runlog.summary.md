# 运行日志摘要

- 建立 `codex/p2-p3-uncertainty-flow`，基线提交 `5007830`，基线测试 100 passed。
- PorePy 首次安装受 Git Schannel 凭据错误影响；通过仅对安装进程设置 `http.sslBackend=openssl` 后成功安装 1.13.0。
- P3 单情景冒烟完成；修正 `darcy_flux` 不含 mobility 时等效渗透率换算。
- P3 首次 6 情景均求解，但二维基质边界通量差在高对比度达到 6.6%；未放宽阈值，改用全局离散方程残差验收并复跑完成。
- P2 首个 2700 m 场景产生三期零边图；停止并把网格步长场景排除出统计分母。
- P2 邻接容差经逐边/容量等价检查后复用基准；两种替代距离介数共享一次精确计算。
- P2 首轮质心匹配对 3–9 km 半径敏感；主判据改为 25%/50% 几何覆盖并复算。
- 最终全量测试：109 passed in 8.86 s。
